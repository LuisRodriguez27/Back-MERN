{
  "name": "back-mern",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:tsc": "tsc -p tsconfig.prod.json",
    "build:clean": "rimraf dist",
    "build": "npm run build:clean && npm run build:tsc",
    "start": "cross-env NODE_ENV=production node dist/src/index.js",
    "dev": "cross-env NODE_ENV=dev ts-node-dev --transpile-only -- ./src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
